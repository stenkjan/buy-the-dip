"""bot_core: reusable framework for indicator-based market signal bots.

Strategy-agnostic. Buy-the-dip lives in `strategies.buy_the_dip` and plugs in
via the `Strategy` protocol in `bot_core.signals.base`.
"""

__version__ = "0.1.0"
